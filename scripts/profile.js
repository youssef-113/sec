document.addEventListener("DOMContentLoaded", () => {
    // Example: Edit profile button
    document.querySelector(".btn-primary").addEventListener("click", () => {
        window.showNotification("Edit profile feature coming soon!", "info")
    })
})
